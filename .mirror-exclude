# Paths stripped from the PUBLIC snapshot pushed to Anjadhe/anjadhe-connect.
# One path per line, relative to repo root; directories end with /.
# The snapshot is built from `git archive <tag>` (tracked files only, no
# history), so anything not listed here becomes public. Edit freely.
#
# NOTE: docs/ needs no listing — the mirror fails on any docs/ file not
# allowlisted in .mirror-public-docs (private by default), and .github/ is
# always stripped wholesale by the workflow itself.

# --- Internal process docs ---
RELEASING.md

# --- Local secret files (untracked, listed defensively) ---
.env
